import { NextRequest, NextResponse } from "next/server";
// TEMP minimal - will replace
export async function GET() { return NextResponse.json({ ok: true }); }
export async function POST() { return NextResponse.json({ ok: true }); }
